const gallerySelect = function () {
    const selectElem = document.querySelector('.js-select');
    const choices = new Choices(selectElem, {
        searchEnabled: false,
        itemSelectText: '',
    })
}